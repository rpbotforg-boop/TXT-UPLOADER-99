# Ultimate TXT → Video Bot (Admin Version)

## Setup
1. Upload bot.py, requirements.txt, .env to server / Colab / VPS
2. Install dependencies:
   pip install -r requirements.txt
3. Set credentials in .env
4. Run:
   python bot.py

## Features
- Multi-TXT batch
- Multi-channel upload
- Batch thumbnail
- Watermark per video
- Progress bar + %
- Schedule uploads
- Admin-only (full authorization)
- Inline buttons for control
- King branding
